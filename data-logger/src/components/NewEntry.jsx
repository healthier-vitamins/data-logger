const NewEntry = ({date, entries, monthDay}) => {
    for (let i=0; i < entries?.rec)


    return (
        <section className={date}>
      <div>{monthDay}</div>
      <Entry
        ticker={values.ticker}
        // trade={trade}
        // price={price}
      />
    </section>
    )
}

export default NewEntry